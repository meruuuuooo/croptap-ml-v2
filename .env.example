# Environment Configuration for CropTap ML API
# Copy this file to .env and update the values

# Application Environment
APP_ENV=production
APP_NAME=CropTap ML API

# Logging
LOG_LEVEL=info

# API Configuration
API_VERSION=v1
API_PREFIX=/api/v1

# CORS Settings (comma-separated origins)
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Model Configuration
MODEL_PATH=/opt/croptap-api/models/crop_suitability_model.pkl

# Redis (if implementing caching)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0

# Monitoring (if implementing)
# SENTRY_DSN=
